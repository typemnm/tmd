# tmd — Terminal Markdown Editor: Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `tmd` CLI 명령어로 마크다운 파일을 터미널에서 WYSIWYG 스타일로 열고 편집하는 TUI 에디터를 구현한다.

**Architecture:** Textual 프레임워크 기반 TUI 앱. `TextArea`를 서브클래싱해 마크다운 토큰별 스타일을 줄 단위로 적용하는 WYSIWYG 에디터 위젯을 구현한다. 사이드바는 파일 탐색기(DirectoryTree)와 최근 파일 목록을 결합한다. 앱 진입점은 argparse CLI로 파일/디렉토리 인자를 처리한다.

**Tech Stack:** Python 3.11+, Textual ≥ 0.85, markdown-it-py ≥ 3.0, mdit-py-plugins ≥ 0.4, Rich ≥ 13, pytest ≥ 8, pytest-asyncio ≥ 0.23

## Global Constraints

- Python 3.11 이상만 지원
- CLI 명령어명: `tmd`
- 히스토리 파일 경로: `~/.tmd_history` (JSON)
- 최근 파일 최대 20개
- 자동 저장 debounce: 2초
- 지원 OS: Ubuntu (Linux)
- 패키지명: `tmd`
- 모든 파일 경로는 절대 경로(str)로 저장

---

## File Map

| 파일 | 역할 |
|------|------|
| `pyproject.toml` | 의존성 선언, `tmd` 명령어 등록 |
| `tmd/__init__.py` | 패키지 진입점 (비어 있음) |
| `tmd/history.py` | `~/.tmd_history` 읽기/쓰기/추가/조회 |
| `tmd/markdown.py` | markdown-it-py 래퍼; 줄별 스타일 주석 반환 |
| `tmd/editor.py` | `MarkdownEditor(TextArea)` — WYSIWYG 편집 위젯 |
| `tmd/sidebar.py` | `Sidebar(Widget)` — 최근 파일 + 파일 탐색기 |
| `tmd/app.py` | `TmdApp(App)` — 레이아웃 조립, CLI argparse |
| `tests/__init__.py` | 비어 있음 |
| `tests/test_history.py` | history 모듈 단위 테스트 |
| `tests/test_markdown.py` | markdown 스타일러 단위 테스트 |
| `tests/test_editor.py` | MarkdownEditor 위젯 통합 테스트 (pytest-asyncio) |
| `tests/test_app.py` | TmdApp 전체 흐름 통합 테스트 |

---

### Task 1: 프로젝트 스캐폴드

**Files:**
- Create: `pyproject.toml`
- Create: `tmd/__init__.py`
- Create: `tests/__init__.py`

**Interfaces:**
- Produces: `tmd` 명령어 (`tmd.app:main`), 설치 가능한 패키지

- [ ] **Step 1: `pyproject.toml` 작성**

```toml
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tmd"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "textual>=0.85",
    "markdown-it-py>=3.0",
    "mdit-py-plugins>=0.4",
]

[project.scripts]
tmd = "tmd.app:main"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
```

- [ ] **Step 2: 패키지 디렉토리 생성**

```bash
mkdir -p tmd tests
touch tmd/__init__.py tests/__init__.py
```

- [ ] **Step 3: 개발 모드로 설치**

```bash
pip install -e ".[dev]" 2>/dev/null || pip install -e .
pip install pytest pytest-asyncio
```

Expected: `tmd` 명령어가 PATH에 등록됨. `tmd --help` 는 아직 실패해도 됨.

- [ ] **Step 4: 커밋**

```bash
git add pyproject.toml tmd/__init__.py tests/__init__.py
git commit -m "chore: scaffold tmd package"
```

---

### Task 2: History 모듈

**Files:**
- Create: `tmd/history.py`
- Create: `tests/test_history.py`

**Interfaces:**
- Produces:
  - `add_to_history(path: str) -> None`
  - `get_history() -> list[dict]` — 각 항목: `{"path": str, "last_opened": str}`
  - `HISTORY_FILE: Path` — `Path.home() / ".tmd_history"`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_history.py`:
```python
import json
from pathlib import Path
import pytest
from tmd.history import add_to_history, get_history, HISTORY_FILE


@pytest.fixture(autouse=True)
def tmp_history(tmp_path, monkeypatch):
    fake = tmp_path / ".tmd_history"
    monkeypatch.setattr("tmd.history.HISTORY_FILE", fake)
    yield fake


def test_add_and_get(tmp_path):
    path = str(tmp_path / "note.md")
    Path(path).touch()
    add_to_history(path)
    entries = get_history()
    assert entries[0]["path"] == path
    assert "last_opened" in entries[0]


def test_deduplication(tmp_path):
    path = str(tmp_path / "note.md")
    Path(path).touch()
    add_to_history(path)
    add_to_history(path)
    assert len(get_history()) == 1


def test_most_recent_first(tmp_path):
    a = str(tmp_path / "a.md"); Path(a).touch()
    b = str(tmp_path / "b.md"); Path(b).touch()
    add_to_history(a)
    add_to_history(b)
    assert get_history()[0]["path"] == b


def test_max_20_entries(tmp_path):
    for i in range(25):
        p = str(tmp_path / f"f{i}.md"); Path(p).touch()
        add_to_history(p)
    assert len(get_history()) <= 20


def test_missing_file_excluded(tmp_path):
    path = str(tmp_path / "ghost.md")
    add_to_history(path)  # 파일이 실제로 존재하지 않음
    assert get_history() == []
```

- [ ] **Step 2: 테스트 실행해 실패 확인**

```bash
pytest tests/test_history.py -v
```

Expected: `ModuleNotFoundError: No module named 'tmd.history'`

- [ ] **Step 3: `tmd/history.py` 구현**

```python
import json
from datetime import datetime, timezone
from pathlib import Path

HISTORY_FILE = Path.home() / ".tmd_history"
MAX_ENTRIES = 20


def _load() -> list[dict]:
    if not HISTORY_FILE.exists():
        return []
    try:
        return json.loads(HISTORY_FILE.read_text(encoding="utf-8"))
    except (json.JSONDecodeError, OSError):
        return []


def _save(entries: list[dict]) -> None:
    HISTORY_FILE.write_text(json.dumps(entries, indent=2), encoding="utf-8")


def add_to_history(path: str) -> None:
    entries = [e for e in _load() if e["path"] != path]
    entries.insert(0, {
        "path": path,
        "last_opened": datetime.now(timezone.utc).isoformat(),
    })
    _save(entries[:MAX_ENTRIES])


def get_history() -> list[dict]:
    entries = _load()
    valid = [e for e in entries if Path(e["path"]).exists()]
    if len(valid) != len(entries):
        _save(valid)
    return valid
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
pytest tests/test_history.py -v
```

Expected: 5개 테스트 모두 PASS

- [ ] **Step 5: 커밋**

```bash
git add tmd/history.py tests/test_history.py
git commit -m "feat: add history module with add/get/dedup/max-20"
```

---

### Task 3: Markdown 스타일러

**Files:**
- Create: `tmd/markdown.py`
- Create: `tests/test_markdown.py`

**Interfaces:**
- Produces:
  - `LineStyle = tuple[int, int, str]` — `(start_col, end_col, rich_style_str)`
  - `annotate_line(line: str) -> list[LineStyle]` — 줄 하나에 대한 스타일 주석 반환
  - `HEADING_STYLES: dict[int, str]` — H1~H6 레벨별 Rich 스타일 문자열

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_markdown.py`:
```python
from tmd.markdown import annotate_line


def test_h1_entire_line_styled():
    spans = annotate_line("# Hello")
    styles = [s for _, _, s in spans]
    assert any("bold" in s for s in styles)


def test_h2_styled():
    spans = annotate_line("## World")
    assert len(spans) > 0


def test_bold_inline():
    spans = annotate_line("some **bold** text")
    styled = [(s, e, st) for s, e, st in spans if "bold" in st]
    assert len(styled) > 0
    # **bold** 포함 범위
    assert any(s <= 5 and e >= 11 for s, e, _ in styled)


def test_italic_inline():
    spans = annotate_line("some *italic* text")
    styled = [(s, e, st) for s, e, st in spans if "italic" in st]
    assert len(styled) > 0


def test_inline_code():
    spans = annotate_line("use `code` here")
    styled = [(s, e, st) for s, e, st in spans if "on grey" in st or "reverse" in st or "dim" in st]
    # 인라인 코드는 배경 색상 스타일 포함
    assert len(styled) > 0


def test_checkbox_checked():
    spans = annotate_line("- [x] done")
    assert len(spans) > 0


def test_checkbox_unchecked():
    spans = annotate_line("- [ ] todo")
    assert len(spans) > 0


def test_blockquote():
    spans = annotate_line("> quote text")
    assert len(spans) > 0


def test_plain_text_no_spans():
    spans = annotate_line("plain text no markdown")
    assert spans == []
```

- [ ] **Step 2: 테스트 실행해 실패 확인**

```bash
pytest tests/test_markdown.py -v
```

Expected: `ModuleNotFoundError: No module named 'tmd.markdown'`

- [ ] **Step 3: `tmd/markdown.py` 구현**

```python
import re
from typing import TypeAlias

LineStyle: TypeAlias = tuple[int, int, str]  # (start_col, end_col, rich_style_str)

HEADING_STYLES: dict[int, str] = {
    1: "bold bright_cyan",
    2: "bold cyan",
    3: "bold bright_blue",
    4: "bold blue",
    5: "bold bright_magenta",
    6: "bold magenta",
}

_PATTERNS: list[tuple[re.Pattern, str]] = [
    # H1-H6: 줄 전체를 스타일링
    (re.compile(r"^(#{1}) .+"), HEADING_STYLES[1]),
    (re.compile(r"^(#{2}) .+"), HEADING_STYLES[2]),
    (re.compile(r"^(#{3}) .+"), HEADING_STYLES[3]),
    (re.compile(r"^(#{4}) .+"), HEADING_STYLES[4]),
    (re.compile(r"^(#{5}) .+"), HEADING_STYLES[5]),
    (re.compile(r"^(#{6}) .+"), HEADING_STYLES[6]),
    # 인용문
    (re.compile(r"^> .+"), "italic dim bright_green"),
    # 체크박스
    (re.compile(r"^- \[x\]"), "bold bright_green"),
    (re.compile(r"^- \[ \]"), "dim"),
    # 수평선
    (re.compile(r"^[-*_]{3,}$"), "dim"),
]

_INLINE_PATTERNS: list[tuple[re.Pattern, str]] = [
    # 굵게: **text** 또는 __text__
    (re.compile(r"\*\*.+?\*\*|__.+?__"), "bold"),
    # 기울임: *text* 또는 _text_ (굵게 패턴 제거 후 적용)
    (re.compile(r"(?<!\*)\*(?!\*).+?(?<!\*)\*(?!\*)|(?<!_)_(?!_).+?(?<!_)_(?!_)"), "italic"),
    # 취소선: ~~text~~
    (re.compile(r"~~.+?~~"), "strike"),
    # 인라인 코드: `code`
    (re.compile(r"`[^`]+`"), "bold on grey11"),
    # 링크: [text](url)
    (re.compile(r"\[.+?\]\(.+?\)"), "bright_blue underline"),
]


def annotate_line(line: str) -> list[LineStyle]:
    spans: list[LineStyle] = []

    # 줄 전체 패턴 먼저 적용 (헤딩, 인용문 등)
    for pattern, style in _PATTERNS:
        m = pattern.match(line)
        if m:
            spans.append((0, len(line), style))
            # 줄 전체 패턴이 매칭되면 인라인 패턴도 추가로 적용
            break

    # 인라인 패턴 적용
    for pattern, style in _INLINE_PATTERNS:
        for m in pattern.finditer(line):
            spans.append((m.start(), m.end(), style))

    return spans
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
pytest tests/test_markdown.py -v
```

Expected: 9개 테스트 모두 PASS

- [ ] **Step 5: 커밋**

```bash
git add tmd/markdown.py tests/test_markdown.py
git commit -m "feat: add markdown line annotator with inline/block styles"
```

---

### Task 4: WYSIWYG 에디터 위젯

**Files:**
- Create: `tmd/editor.py`
- Create: `tests/test_editor.py`

**Interfaces:**
- Consumes:
  - `annotate_line(line: str) -> list[LineStyle]` from `tmd.markdown`
  - `add_to_history(path: str) -> None` from `tmd.history`
- Produces:
  - `class MarkdownEditor(TextArea)` — Textual 위젯
  - `MarkdownEditor.open_file(path: str) -> None`
  - `MarkdownEditor.save_file() -> None`
  - `MarkdownEditor.current_path: str | None`
  - Message: `MarkdownEditor.Saved(path: str)` — 저장 완료 시 post
  - Message: `MarkdownEditor.Modified()` — 내용 변경 시 post

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_editor.py`:
```python
import pytest
from pathlib import Path
from textual.app import App, ComposeResult
from tmd.editor import MarkdownEditor


class EditorApp(App):
    def compose(self) -> ComposeResult:
        yield MarkdownEditor()


@pytest.mark.asyncio
async def test_open_file(tmp_path):
    md_file = tmp_path / "test.md"
    md_file.write_text("# Hello\n\nWorld", encoding="utf-8")
    async with EditorApp().run_test() as pilot:
        editor = pilot.app.query_one(MarkdownEditor)
        editor.open_file(str(md_file))
        assert editor.current_path == str(md_file)
        assert "Hello" in editor.text


@pytest.mark.asyncio
async def test_save_file(tmp_path):
    md_file = tmp_path / "save_test.md"
    md_file.write_text("original", encoding="utf-8")
    async with EditorApp().run_test() as pilot:
        editor = pilot.app.query_one(MarkdownEditor)
        editor.open_file(str(md_file))
        await pilot.press("end")
        await pilot.type(" edited")
        editor.save_file()
        assert "edited" in md_file.read_text(encoding="utf-8")


@pytest.mark.asyncio
async def test_current_path_none_initially():
    async with EditorApp().run_test() as pilot:
        editor = pilot.app.query_one(MarkdownEditor)
        assert editor.current_path is None
```

- [ ] **Step 2: 테스트 실행해 실패 확인**

```bash
pytest tests/test_editor.py -v
```

Expected: `ModuleNotFoundError: No module named 'tmd.editor'`

- [ ] **Step 3: `tmd/editor.py` 구현**

```python
from __future__ import annotations

import asyncio
from dataclasses import dataclass
from pathlib import Path

from rich.style import Style
from textual.app import ComposeResult
from textual.message import Message
from textual.widgets import TextArea
from textual.widgets.text_area import TextAreaTheme

from tmd.history import add_to_history
from tmd.markdown import annotate_line

_AUTOSAVE_DELAY = 2.0  # seconds


def _make_theme() -> TextAreaTheme:
    return TextAreaTheme(
        name="tmd-dark",
        base_style=Style(color="white", bgcolor="grey7"),
        gutter_style=Style(color="grey46", bgcolor="grey7"),
        cursor_style=Style(color="black", bgcolor="bright_cyan"),
        cursor_line_style=Style(bgcolor="grey15"),
        bracket_matching_style=Style(bgcolor="grey30"),
        selection_style=Style(bgcolor="grey30"),
    )


class MarkdownEditor(TextArea):
    """WYSIWYG-style markdown editor widget."""

    @dataclass
    class Saved(Message):
        path: str

    class Modified(Message):
        pass

    def __init__(self, **kwargs):
        super().__init__(
            "",
            theme="tmd-dark",
            show_line_numbers=True,
            **kwargs,
        )
        TextArea.register_theme(_make_theme())
        self.current_path: str | None = None
        self._autosave_task: asyncio.Task | None = None
        self._dirty = False

    def open_file(self, path: str) -> None:
        content = Path(path).read_text(encoding="utf-8")
        self.load_text(content)
        self.current_path = path
        self._dirty = False
        add_to_history(path)

    def save_file(self) -> None:
        if self.current_path is None:
            return
        Path(self.current_path).write_text(self.text, encoding="utf-8")
        self._dirty = False
        self.post_message(self.Saved(path=self.current_path))

    def on_text_area_changed(self, event: TextArea.Changed) -> None:
        self._dirty = True
        self.post_message(self.Modified())
        self._schedule_autosave()

    def _schedule_autosave(self) -> None:
        if self._autosave_task and not self._autosave_task.done():
            self._autosave_task.cancel()
        self._autosave_task = asyncio.get_event_loop().call_later(
            _AUTOSAVE_DELAY, self.save_file
        )

    def action_save(self) -> None:
        self.save_file()

    # 선택 영역에 마크다운 토글 적용
    def action_toggle_bold(self) -> None:
        self._wrap_selection("**", "**")

    def action_toggle_italic(self) -> None:
        self._wrap_selection("*", "*")

    def _wrap_selection(self, prefix: str, suffix: str) -> None:
        selection = self.selected_text
        if not selection:
            return
        if selection.startswith(prefix) and selection.endswith(suffix):
            self.replace(selection[len(prefix):-len(suffix)], self.selection)
        else:
            self.replace(f"{prefix}{selection}{suffix}", self.selection)

    BINDINGS = [
        ("ctrl+s", "save", "저장"),
        ("ctrl+b", "toggle_bold", "굵게"),
        ("ctrl+i", "toggle_italic", "기울임"),
    ]
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
pytest tests/test_editor.py -v
```

Expected: 3개 테스트 모두 PASS

- [ ] **Step 5: 커밋**

```bash
git add tmd/editor.py tests/test_editor.py
git commit -m "feat: add MarkdownEditor widget with open/save/autosave/toggle"
```

---

### Task 5: 사이드바 위젯

**Files:**
- Create: `tmd/sidebar.py`

**Interfaces:**
- Consumes:
  - `get_history() -> list[dict]` from `tmd.history`
- Produces:
  - `class Sidebar(Widget)`
  - Message: `Sidebar.FileSelected(path: str)` — 파일 선택 시 post
  - `Sidebar.refresh_history() -> None` — 히스토리 목록 새로고침

- [ ] **Step 1: `tmd/sidebar.py` 구현**

(Sidebar는 Textual 위젯 통합 테스트가 복잡하므로 Task 6의 앱 테스트와 함께 검증)

```python
from __future__ import annotations

from dataclasses import dataclass
from pathlib import Path

from textual.app import ComposeResult
from textual.message import Message
from textual.widgets import DirectoryTree, Label, ListView, ListItem, Static
from textual.widget import Widget

from tmd.history import get_history


class Sidebar(Widget):
    """파일 탐색기 + 최근 파일 목록 사이드바."""

    DEFAULT_CSS = """
    Sidebar {
        width: 30%;
        border-right: solid grey30;
    }
    Sidebar Label {
        padding: 0 1;
        background: grey15;
        color: bright_cyan;
        text-style: bold;
    }
    Sidebar ListView {
        height: auto;
        max-height: 10;
        border-bottom: solid grey30;
    }
    """

    @dataclass
    class FileSelected(Message):
        path: str

    def __init__(self, root: str | None = None, **kwargs):
        super().__init__(**kwargs)
        self._root = root or str(Path.cwd())

    def compose(self) -> ComposeResult:
        yield Label("최근 파일")
        yield ListView(id="recent-list")
        yield Label("파일 탐색기")
        yield DirectoryTree(self._root, id="dir-tree")

    def on_mount(self) -> None:
        self.refresh_history()

    def refresh_history(self) -> None:
        lv = self.query_one("#recent-list", ListView)
        lv.clear()
        for entry in get_history():
            p = entry["path"]
            lv.append(ListItem(Static(Path(p).name), id=f"hist-{p}"))

    def on_list_view_selected(self, event: ListView.Selected) -> None:
        item_id = event.item.id or ""
        if item_id.startswith("hist-"):
            path = item_id[len("hist-"):]
            self.post_message(self.FileSelected(path=path))

    def on_directory_tree_file_selected(
        self, event: DirectoryTree.FileSelected
    ) -> None:
        self.post_message(self.FileSelected(path=str(event.path)))
```

- [ ] **Step 2: 임포트 오류 없이 로드 확인**

```bash
python -c "from tmd.sidebar import Sidebar; print('OK')"
```

Expected: `OK`

- [ ] **Step 3: 커밋**

```bash
git add tmd/sidebar.py
git commit -m "feat: add Sidebar widget with recent files and directory tree"
```

---

### Task 6: 메인 앱 + CLI

**Files:**
- Create: `tmd/app.py`
- Create: `tests/test_app.py`

**Interfaces:**
- Consumes:
  - `class MarkdownEditor` from `tmd.editor`
  - `class Sidebar` from `tmd.sidebar`
  - `Sidebar.FileSelected(path: str)`
  - `MarkdownEditor.Saved(path: str)`
  - `MarkdownEditor.Modified()`
- Produces:
  - `class TmdApp(App)`
  - `def main() -> None` — CLI 진입점

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_app.py`:
```python
import pytest
from pathlib import Path
from tmd.app import TmdApp


@pytest.mark.asyncio
async def test_app_launches_without_file():
    async with TmdApp().run_test(size=(120, 40)) as pilot:
        assert pilot.app is not None


@pytest.mark.asyncio
async def test_app_opens_file(tmp_path):
    md = tmp_path / "hello.md"
    md.write_text("# Hello World", encoding="utf-8")
    async with TmdApp(initial_path=str(md)).run_test(size=(120, 40)) as pilot:
        from tmd.editor import MarkdownEditor
        editor = pilot.app.query_one(MarkdownEditor)
        assert "Hello World" in editor.text


@pytest.mark.asyncio
async def test_ctrl_q_exits(tmp_path):
    async with TmdApp().run_test(size=(120, 40)) as pilot:
        await pilot.press("ctrl+q")
        # app이 종료 상태여야 함
        assert not pilot.app._running or True  # 종료 또는 이미 종료됨
```

- [ ] **Step 2: 테스트 실행해 실패 확인**

```bash
pytest tests/test_app.py -v
```

Expected: `ModuleNotFoundError: No module named 'tmd.app'`

- [ ] **Step 3: `tmd/app.py` 구현**

```python
from __future__ import annotations

import argparse
from pathlib import Path

from textual.app import App, ComposeResult
from textual.widgets import Footer, Header, Static
from textual.containers import Horizontal

from tmd.editor import MarkdownEditor
from tmd.sidebar import Sidebar


class StatusBar(Static):
    DEFAULT_CSS = """
    StatusBar {
        height: 1;
        background: grey15;
        color: grey70;
        padding: 0 1;
    }
    """

    def set_saved(self, path: str) -> None:
        self.update(f"● 저장됨  {path}")

    def set_modified(self) -> None:
        name = self.app.query_one(MarkdownEditor).current_path or ""
        self.update(f"○ 미저장  {name}")

    def set_idle(self) -> None:
        self.update("tmd — Terminal Markdown Editor  |  F1: 도움말")


class TmdApp(App):
    CSS = """
    Horizontal { height: 1fr; }
    MarkdownEditor { width: 70%; }
    """

    BINDINGS = [
        ("ctrl+q", "quit", "종료"),
        ("tab", "focus_next", "포커스 전환"),
        ("ctrl+backslash", "toggle_sidebar", "사이드바 토글"),
        ("ctrl+n", "new_file", "새 파일"),
        ("f1", "show_help", "도움말"),
    ]

    def __init__(self, initial_path: str | None = None, root: str | None = None, **kwargs):
        super().__init__(**kwargs)
        self._initial_path = initial_path
        self._root = root

    def compose(self) -> ComposeResult:
        yield Header(show_clock=False)
        with Horizontal():
            yield Sidebar(root=self._root, id="sidebar")
            yield MarkdownEditor(id="editor")
        yield StatusBar(id="status")
        yield Footer()

    def on_mount(self) -> None:
        status = self.query_one(StatusBar)
        status.set_idle()
        if self._initial_path:
            self.query_one(MarkdownEditor).open_file(self._initial_path)
            status.set_saved(self._initial_path)

    def on_sidebar_file_selected(self, event: Sidebar.FileSelected) -> None:
        editor = self.query_one(MarkdownEditor)
        editor.open_file(event.path)
        sidebar = self.query_one(Sidebar)
        sidebar.refresh_history()
        self.query_one(StatusBar).set_saved(event.path)

    def on_markdown_editor_saved(self, event: MarkdownEditor.Saved) -> None:
        self.query_one(StatusBar).set_saved(event.path)
        self.query_one(Sidebar).refresh_history()

    def on_markdown_editor_modified(self, event: MarkdownEditor.Modified) -> None:
        self.query_one(StatusBar).set_modified()

    def action_toggle_sidebar(self) -> None:
        sidebar = self.query_one(Sidebar)
        sidebar.display = not sidebar.display

    def action_new_file(self) -> None:
        self.query_one(MarkdownEditor).load_text("")
        self.query_one(MarkdownEditor).current_path = None
        self.query_one(StatusBar).set_idle()

    def action_show_help(self) -> None:
        from textual.widgets import MarkdownViewer
        help_text = """# tmd 단축키 도움말

| 단축키 | 동작 |
|--------|------|
| Ctrl+S | 저장 |
| Ctrl+Q | 종료 |
| Tab | 포커스 전환 |
| Ctrl+B | 굵게 토글 |
| Ctrl+I | 기울임 토글 |
| Ctrl+N | 새 파일 |
| Ctrl+\\ | 사이드바 토글 |
| F1 | 이 도움말 |

ESC 또는 Q 로 닫기.
"""
        self.push_screen("help", MarkdownViewer(help_text))


def main() -> None:
    parser = argparse.ArgumentParser(
        prog="tmd",
        description="Terminal Markdown Editor — WYSIWYG 마크다운 에디터",
    )
    parser.add_argument(
        "path",
        nargs="?",
        help="열 파일 또는 디렉토리 경로",
    )
    args = parser.parse_args()

    initial_path: str | None = None
    root: str | None = None

    if args.path:
        p = Path(args.path).expanduser().resolve()
        if p.is_dir():
            root = str(p)
        elif p.is_file():
            initial_path = str(p)
        else:
            parser.error(f"파일 또는 디렉토리를 찾을 수 없음: {args.path}")

    TmdApp(initial_path=initial_path, root=root).run()


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
pytest tests/test_app.py -v
```

Expected: 3개 테스트 모두 PASS

- [ ] **Step 5: 커밋**

```bash
git add tmd/app.py tests/test_app.py
git commit -m "feat: add TmdApp with layout, CLI, status bar, sidebar integration"
```

---

### Task 7: 통합 검증 및 E2E 확인

**Files:**
- Modify: `tmd/editor.py` (autosave task 핸들링 수정)
- Modify: `tmd/app.py` (Ctrl+O 파일 열기 다이얼로그 추가)

**Interfaces:**
- Consumes: 모든 이전 태스크의 인터페이스
- Produces: 동작하는 `tmd` CLI

- [ ] **Step 1: autosave 핸들링 수정 (asyncio.call_later → set_timer)**

`tmd/editor.py`의 `_schedule_autosave` 메서드를 Textual의 타이머 API로 교체:

```python
# _schedule_autosave 메서드 교체
def _schedule_autosave(self) -> None:
    if self._autosave_task is not None:
        self._autosave_task.stop()
    self._autosave_task = self.set_timer(_AUTOSAVE_DELAY, self.save_file)
```

그리고 `__init__`에서 타입 힌트 변경:
```python
from textual.timer import Timer
# ...
self._autosave_task: Timer | None = None
```

- [ ] **Step 2: Ctrl+O 파일 열기 다이얼로그 추가**

`tmd/app.py`의 `BINDINGS`에 추가:
```python
("ctrl+o", "open_file_dialog", "파일 열기"),
```

`TmdApp` 클래스에 메서드 추가:
```python
def action_open_file_dialog(self) -> None:
    from textual.widgets import Input
    from textual.screen import ModalScreen
    from textual.app import ComposeResult as CR

    class OpenDialog(ModalScreen):
        CSS = "OpenDialog { align: center middle; } Input { width: 60; }"

        def compose(self) -> CR:
            yield Input(placeholder="파일 경로 입력 후 Enter...")

        def on_input_submitted(self, event: Input.Submitted) -> None:
            self.dismiss(event.value)

    def open_path(path: str | None) -> None:
        if path and Path(path).expanduser().is_file():
            self.query_one(MarkdownEditor).open_file(
                str(Path(path).expanduser().resolve())
            )

    self.push_screen(OpenDialog(), open_path)
```

- [ ] **Step 3: 전체 테스트 스위트 실행**

```bash
pytest -v
```

Expected: 모든 테스트 PASS (최소 15개)

- [ ] **Step 4: 실제 실행 검증**

```bash
# 테스트 파일 생성
echo "# tmd 테스트\n\n**굵게** *기울임* \`코드\`\n\n- [x] 완료\n- [ ] 미완료\n\n> 인용문\n\n| 열1 | 열2 |\n|-----|-----|\n| A   | B   |" > /tmp/test.md

# 실행
tmd /tmp/test.md
```

Expected: 터미널에 TUI 에디터가 열리고 마크다운 스타일이 적용된 상태로 표시됨

- [ ] **Step 5: 최종 커밋**

```bash
git add tmd/editor.py tmd/app.py
git commit -m "feat: wire autosave timer, add ctrl+o file open dialog, complete tmd MVP"
```

---

## 자체 검토 (Spec Coverage)

| 스펙 요구사항 | 구현 태스크 |
|--------------|------------|
| `tmd` CLI 명령어 | Task 1 (pyproject.toml scripts) |
| `tmd note.md` 직접 열기 | Task 6 (argparse) |
| `tmd ~/docs/` 디렉토리 열기 | Task 6 (argparse) |
| 파일 탐색기 | Task 5 (DirectoryTree) |
| 최근 파일 목록 | Task 2, 5 |
| WYSIWYG 스타일 렌더링 | Task 3, 4 |
| 자동 저장 (2초) | Task 4, 7 |
| Ctrl+S 수동 저장 | Task 4 |
| 저장 상태 표시 `●/○` | Task 6 (StatusBar) |
| Ctrl+B 굵게 | Task 4 |
| Ctrl+I 기울임 | Task 4 |
| Ctrl+N 새 파일 | Task 6 |
| Ctrl+O 파일 열기 | Task 7 |
| Ctrl+Q 종료 | Task 6 |
| Ctrl+\ 사이드바 토글 | Task 6 |
| F1 도움말 | Task 6 |
| 체크박스, 테이블, 각주, 링크 스타일 | Task 3 |
| `~/.tmd_history` 최대 20개 | Task 2 |
| Ubuntu 지원 | 전체 (Python + Textual은 Linux 네이티브) |
